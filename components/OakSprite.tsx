'use client'

// OakSprite is now used as the full-screen background for Oak scenes.
// frame_011.jpg has Oak centered on a white background — perfect.
// This component is kept for layout compatibility; Oak scenes in page.tsx
// now render the frame as a background <img> directly.

export default function OakSprite() {
  // Renders nothing — Oak scenes now use the frame image as background
  return null
}
